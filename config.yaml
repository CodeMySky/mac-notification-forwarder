sys:
  logging_level: "INFO"
  logging_file: "history.log"
forwarders:
  slack:
    channel: "C04HHD8RW6M"
router:
  # Only send message when system is locked
  locked_only: true
  block_list:
    - "com.apple.MobileSMS"